import React from "react";
import { Link } from 'react-router-dom'

const Resprof =({Name, Opens_at,desc, imageKey, key, city, stateId, countryId}) => {
    return (
        <div className = "card_res">
        {/* title={Name}
        subheader= {Opens_at}      
        {imageKey && 
        <div>
        img={`http://localhost:8081/images/${imageKey}`}
        alt="Paella dish"
       <div/>
      } */}
      </div>
    )
}

export default Resprof;